describe("testing employees model", () => {
    beforeAll(() => {
        // Initialize database to base values
    });
    test("adds a new employee", () => {});

    test("removes an existing employee", () => {});
});
